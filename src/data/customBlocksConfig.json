[
  {
    "name": "myapp/info-card",
    "title": "Info Card",
    "description": "A simple card with heading and body — registered from JSON config (simulates DB-loaded blocks)",
    "category": "myapp-blocks",
    "icon": "info",
    "keywords": ["card", "info", "custom"],
    "attributes": {
      "cardTitle": { "type": "string", "default": "Card Title" },
      "cardBody":  { "type": "string", "default": "Write a short description for this info card block..." },
      "accentColor": { "type": "string", "default": "#3858e9" }
    }
  },
  {
    "name": "myapp/badge",
    "title": "Badge / Label",
    "description": "A small inline badge or status label — registered from JSON config",
    "category": "myapp-blocks",
    "icon": "tag",
    "keywords": ["badge", "label", "tag", "chip"],
    "attributes": {
      "text":      { "type": "string", "default": "New" },
      "badgeColor": { "type": "string", "default": "#3858e9" }
    }
  },
  {
    "name": "myapp/section-divider",
    "title": "Section Divider",
    "description": "A section heading with a colored underline divider — registered from JSON config",
    "category": "myapp-blocks",
    "icon": "minus",
    "keywords": ["section", "divider", "heading", "separator"],
    "attributes": {
      "heading":      { "type": "string", "default": "Section Title" },
      "dividerColor": { "type": "string", "default": "#3858e9" }
    }
  },
  {
    "name": "myapp/testimonial",
    "title": "Testimonial",
    "description": "A customer testimonial / quote block — registered from JSON config",
    "category": "myapp-blocks",
    "icon": "format-quote",
    "keywords": ["testimonial", "quote", "review", "customer"],
    "attributes": {
      "quote":       { "type": "string", "default": "This product changed the way our team works. Highly recommended!" },
      "author":      { "type": "string", "default": "Jane Doe, Product Manager" },
      "accentColor": { "type": "string", "default": "#3858e9" }
    }
  }
]
